FROM python:3.11-slim

WORKDIR /app

COPY requirements-api.txt .
RUN pip install --no-cache-dir -r requirements-api.txt

COPY . .

# Default: stream telemetry at 1 pkt/s to the api service
CMD ["python", "-m", "scripts.send_telemetry", "--rate", "1", "--api-url", "http://api:8000/telemetry"]
